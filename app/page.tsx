import HomePage from "@/components/pages/public/home/home";

/**
 * A página principal da aplicação.
 * Ela renderiza o componente HomePage.
 *
 * @returns {React.ReactElement} A página inicial renderizada.
 */
export default function Home() {
  return <HomePage />;
}
