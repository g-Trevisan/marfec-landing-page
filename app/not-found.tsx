/**
 * Um componente a ser exibido quando uma página não é encontrada (erro 404).
 *
 * @returns {React.ReactElement} A página 404 renderizada.
 */
export default function NotFound() {
  return <h1>404</h1>;
}
